/**
 * The shortest password the portal accepts, stated in a module with no
 * server-only imports so the setup wizard, the account screens and the
 * change-password form can all agree on it — the browser for an instant
 * answer, the server for the decision.
 */
export const MIN_PASSWORD_LENGTH = 10;
